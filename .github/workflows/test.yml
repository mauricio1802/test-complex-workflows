on:
  push:
    branches:
      - not-trigger-branch

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Set output
          id: vars
          run: echo $(echo $GITHUB_REF | cut -d '/' -f 3)
      # - name: Set output
      #     id: vars
      #     run: echo ::set-output name=short_ref::${}
      # - name: test
      #     if: ${{ steps.vars.outputs.short_ref }} == test
      #     run: echo "🎉 The job was automatically triggered by
