on:
  push:
    branches:
      - @test/trigger

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Set env var
        run: echo $(echo $GITHUB_REF | cut -d'/' -f 3)
